{
  "name": "homunculus",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Multi-agent TTRPG core — deterministic engine behind Agent / SealDice / Substrate ports.",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.159",
    "@anthropic-ai/sdk": "^0.100.1",
    "discord.js": "^14.26.4",
    "zod": "^4.4.3"
  }
}
