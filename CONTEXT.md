# Homunculus v2 — CONTEXT

> 本文件是单 context 的领域文档，描述 v2 的**领域语言**与**架构骨架**。承重的、有争议的决策各自落在 `docs/adr/`。
> v2 在 `v2` 分支上是一次"基本等于新产品"的重构；与 main 上的 v1（单角色卡 Discord bot）**不共享设计**。

## 这是什么

一个由 **AI Agent 驱动的完整 TTRPG 系统**——不只是角色卡，而是整张牌桌。核心赌注是一套**多智能体系统**：把"扮演整个队伍"的负担从单个 AIDM 拆散到多个独立 Agent，**最大化注意力资源的利用**。

### 终极愿景（三支柱）

1. 良好的 VTT 集成。
2. 为不同的 NPC 赋予**持久化的个性**。
3. **多人共用**。

### 第一性原理

> 不让任何一个 LLM 同时既当世界、又当导演、还当一群演员。让 **AIDM be AIDM**（专注叙事/环境），让**每个 NPC/AI 队友各有独立 Agent**（专注"我这个人此刻会怎么做"）。

### v1 范围（当前重构目标）

**只验证多智能体核心**。文字基底，**不做 VTT**（pillar 1 延后）、**不做完整多人**（pillar 3 延后，但架构不得封死它）。理由：多智能体是唯一未经验证的核心赌注；VTT 和多人是相对成熟的工程问题，先上会在没验证核心价值时背上巨大复杂度。见 [ADR-0001]。

## 领域语言（Ubiquitous Language）

| 术语 | 含义 |
|---|---|
| **AIDM** | AI 主持人 Agent。循剧本（忠实呈现 + 应对偏离 + 把大方向拽回剧情，超逆天骰运除外）。是**唯一能写权威叙事状态的翻译官**，独揽机械裁决。 |
| **NPC Agent** | 一个 AI 扮演者，独立 Agent + 独立上下文，满注意力扮演单个角色。分三层（晋升=备团决定，见 [ADR-0010]）：**①队友**（持久长活）、**②核心 NPC**（按需 spin-up、用完即焚）、**③一次性 NPC**（不开 agent，DM 行内口述）。①②统一经 `NpcPort` 表达，引擎按拍拉起、发言按先攻序串行；agent 间"通信"=场景记录而非侧信道。 |
| **灵魂 (soul)** | 一个持久角色实体 = 成长(面板) + 自传体记忆 + 人格核心。**跨桌持久**，独立于"谁在操控它"。 |
| **操控者 (controller)** | 开某个灵魂方向盘的主体：一个真人用户，或一个 NPC Agent。可**显式换手**。 |
| **人格核心 (persona core)** | 灵魂里那块结构化的身份对象：性格内核 + 当前目标 + 关系账 + 剧情进度摘要。永远常驻 Agent 上下文以防失忆/OOC。 |
| **场景 (scene)** | 一等公民状态对象，含"在场名单"。**可见性的单位**：场景记录只对其成员可见。私频 = 成员很少的场景。 |
| **拍 (beat)** | 推进的最小单位。由屏障界定。 |
| **简化战斗轮 (simplified combat round)** | 统一节奏模型：所有场景都当成战斗轮——有出手顺序（战斗投先攻 / 日常 AIDM 点名）、后手看前手刚做的事、效果留存、真人是闸、**最多溢出一轮后 hang**。见 [ADR-0003]。 |
| **屏障 (barrier)** | 节奏机制：AIDM 经工具 `await_actors` 抛出需回应的节点，引擎跑一轮简化战斗轮，被等真人沉默则**溢出一轮后无限 hold = 暂停/存档状态**。见 [ADR-0003]。 |
| **MCP 工具裁判 (engine as MCP referee)** | 引擎把不变量（屏障/可见性/canonicity/检定…）暴露为进程内 MCP 工具；DM/NPC agent 调工具，引擎只裁判。共治/暂停/唯一写权威成为**工具边界物理事实**。见 [ADR-0009]。 |
| **canonicity** | "这一段算不算数"的开关。成长 + 记忆 + 人格演化捆在同一开关上，按 git 分支 fork/merge/discard。 |
| **共治** | Agent 出叙事，确定性引擎掌权威状态。见 [ADR-0002]。 |
| **风味级映射** | 后端是真实的逻辑模型（git 分支/场景/canonicity）；Discord 只做出"风味"，不必把每个构造都可视化。 |
| **剧情脊柱 (spine)** | AIDM 循的"剧本"：一串承重的里程碑 beat，节点之间留给即兴。见 [ADR-0007]。 |
| **里程碑 (milestone)** | 脊柱上的承重节点（"这件事必须发生"）；细节 JIT 充实。 |
| **世界时钟 (world clock)** | 隐藏的进度轨（反派计划/沦陷度）；玩家拖延时推进，AIDM 用来施加"软引力"。 |
| **量子挪移** | 把准备好的内容搬到玩家所在处以保住里程碑；只用于导航选择，绝不用于后果性叙事抉择。 |

## 架构骨架

```
Discord（基底；webhook=人格分身，thread=场景）
        │
        ▼
Agent 层（Agent SDK 驱动）
 ├─ DM Agent：长活自驱 query 循环；调引擎工具推动牌桌；唯一叙事→状态翻译官
 └─ NPC Agents：每灵魂一个，独立上下文；引擎带节奏拉起；纯叙事 + 反思 pass
        │ 调工具 mcp__engine__*（narrate/await_actors/call_check/advance_milestone…）
        ▼
TS 引擎（v2 主体）= 进程内 MCP 工具裁判 + 共享状态
 ├─ 确定性核心：场景/在场、屏障/简化战斗轮（"节奏闸"坍缩进此处）、git 分支/canonicity、路由
 ├─ 廉价两段唤醒闸（拉起 NPC 前判"有话说吗"）
 └─ 记忆库：两层（常驻人格核心 + 情节召回），分支感知
        │ 端口
        ▼
SealDice（Go 边车，HTTP/IPC）
 └─ 角色卡 + 骰子 + 按系统(DND/COC)规则的唯一写权威；DM 只读（无 write_card 工具）
```

- **技术栈与骰子集成**：TS 主体 + SealDice Go 边车。见 [ADR-0001]。
- **共治与纯叙事**：玩家/NPC 纯叙事；AIDM 独写状态；骰子是显式 SealDice 命令的口子。落地为工具权限（NPC 无 narrate/推进权；DM 对卡只读）。见 [ADR-0002]、[ADR-0009]。
- **Agent SDK 驱动 + 引擎即 MCP 裁判**：DM 自驱、NPC 引擎带节奏；引擎能力=进程内 MCP 工具；复用订阅鉴权。见 [ADR-0009]。
- **节奏模型**：简化战斗轮（后手看前手 + 真人是闸 + 溢出一轮即暂停）。见 [ADR-0003]。
- **持久灵魂与 canonicity**：git fork/merge 模型；两层记忆；人格全演化 + 护栏。见 [ADR-0004]。
- **场景=可见性 / 兵分两路=隔离分支**：见 [ADR-0005]。
- **灵魂/操控者解耦 + 换手**：见 [ADR-0006]。
- **AIDM 剧情脊柱（里程碑骨架 + 软引力 + 隐藏世界时钟 + 盲盒开团）**：见 [ADR-0007]。
- **情节记忆召回 v1（最近性 + 关键词，接口可替换为向量库）**：见 [ADR-0008]。
- **Agent SDK 驱动 + 引擎即 MCP 工具裁判（控制权半反转）**：见 [ADR-0009]。
- **Integration 运行时（Discord 可玩化 + NPC 三层模型 + 真人 gateway 等待）**：见 [ADR-0010]。

## 寄存待啃（尚未设计）

- SealDice 的"分支快照垫片"：fork/discard/merge 时如何同步 SealDice 持有的卡状态（SealDice 不懂我们的分支）。
- Day-0 建卡/导卡/灵魂诞生的具体流程。
- **订阅鉴权实测**：Agent SDK 是否认 `CLAUDE_CODE_OAUTH_TOKEN`（不认则回退 API key）。见 [ADR-0009]。
- **锚点问题**：AI 比真人快/准、把结论摆上桌可能架空真人；由"溢出一轮"缓冲 + "一轮"粒度旋钮调。见 [ADR-0003]。
- pillar 1（VTT）、pillar 3（完整多人）——核心验证后再开。

## 已标记的最大风险

**AIDM 机械裁决负担**：纯叙事让 AIDM 重新成为所有机械裁决的瓶颈（解读散文、裁碰撞、喊检定、叙事、拽剧情）。双重缓解：① 把按系统判定整块卸给 SealDice；② "扮演整个队伍"已被结构性拆给 NPC Agents。赌注：AIDM 即使独揽机械裁决也忙得过来。

## Playtest 旋钮（v1 要去调，不是现在拍死）

- 廉价唤醒闸的"说/过"灵敏度。
- 简化战斗轮"一轮"的粒度（粗→桌子活但真人易被锚定；细→真人能动性强但 AI 更受限）。
- 反思 pass 的频率与力度。
